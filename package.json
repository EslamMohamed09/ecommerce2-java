{
  "devDependencies": {
    "sass": "^1.101.0"
  },
  "scripts": {
    "build-css":"sass --no-source-map assets/styles:assets/styles",
    "watch-css":"sass --watch --no-source-map --watch assets/styles:assets/styles",
    "serve": "browser-sync start --server --files 'assets/styles/*.css, *.html'"
  }
}
